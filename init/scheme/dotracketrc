;; please run below with shell terminal first:
;; raco pkg install xrepl ansi-color scribble


(require ansi-color)

(parameterize ([foreground-color 'b-red])
  (color-display "\n Happy "))

(parameterize ([foreground-color 'blue])
  (color-display "Hacking "))

(parameterize ([foreground-color 'green])
  (color-display (string-titlecase (getenv "USER"))))

(parameterize ([foreground-color 'yellow])
  (color-display " - "))

(parameterize ([foreground-color 'cyan])
  (color-display "Racket"))

(parameterize ([foreground-color 'magenta])
  (color-display " ♥ "))

(parameterize ([foreground-color 'b-magenta])
  (color-display "You"))

(parameterize ([foreground-color 'red])
  (color-display " !!!\n\n"))



;; https://docs.racket-lang.org/xrepl/index.html?q=%2Crequire
;;(require xrepl) ;; xrepl can not work very well with geiser

(require sugar)


;; enable at-exp
;; https://docs.racket-lang.org/scribble/reader-internals.html?q=at%20express#%28part._at-exp-lang%29
;; raco pkg install at-exp-lib
(require scribble/reader)
(use-at-readtable)


;; https://docs.racket-lang.org/describe/index.html
;; raco pkg install desscribe
(require describe)

